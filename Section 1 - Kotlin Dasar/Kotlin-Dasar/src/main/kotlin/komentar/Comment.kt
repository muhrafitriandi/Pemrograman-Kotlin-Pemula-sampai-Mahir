package komentar

/**
 * This function will sum the two numbers.
 * @param 1 and 2 are numbers with Int data type.
 * @return Int value of the sum.
 */
fun sum(x: Int, y: Int): Int = x.plus(y)
fun main() {
    println(sum(5, 5)) // call sum function
}